apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-tls
  namespace: kubernetes-dashboard
spec:
  secretName: dashboard-tls-secret
  dnsNames:
    - dashboard.lab.local
  issuerRef:
    name: lab-ca-issuer
    kind: ClusterIssuer
